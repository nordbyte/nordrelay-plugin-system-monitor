{
  "id": "system-monitor",
  "name": "System Monitor",
  "version": "0.1.0",
  "description": "Collects local CPU, memory, disk, and network metrics and renders a peer dashboard.",
  "author": "Ricardo <github@nordbyte.de>",
  "homepage": "https://github.com/nordbyte/nordrelay-plugin-system-monitor",
  "repository": "https://github.com/nordbyte/nordrelay-plugin-system-monitor",
  "license": "MIT",
  "nordrelay": ">=0.9.8",
  "entry": "index.js",
  "permissions": [
    "runtime.read",
    "peers.read",
    "system.metrics.read"
  ],
  "capabilities": {
    "collectors": [
      {
        "id": "system.sample",
        "title": "Collect system metrics",
        "description": "Samples CPU, memory, disk, and network usage on this node.",
        "intervalMs": 5000,
        "runOnStart": true,
        "timeoutMs": 3000
      }
    ],
    "commands": [
      {
        "name": "sample",
        "title": "Collect sample",
        "description": "Collect and persist one metrics sample.",
        "timeoutMs": 5000
      },
      {
        "name": "latest",
        "title": "Latest metrics",
        "description": "Return the latest sample, collecting one if needed.",
        "timeoutMs": 5000
      },
      {
        "name": "history",
        "title": "Metrics history",
        "description": "Return downsampled metrics history for charts.",
        "inputSchema": {
          "type": "object",
          "properties": {
            "range": {
              "type": "string",
              "title": "Range",
              "default": "1h"
            },
            "maxPoints": {
              "type": "number",
              "title": "Max chart points",
              "default": 240
            }
          }
        },
        "timeoutMs": 5000
      },
      {
        "name": "panel-data",
        "title": "Panel data",
        "description": "Return current metrics, summaries, storage status, and chart-ready history.",
        "inputSchema": {
          "type": "object",
          "properties": {
            "range": {
              "type": "string",
              "title": "Range",
              "default": "1h"
            },
            "maxPoints": {
              "type": "number",
              "title": "Max chart points",
              "default": 240
            }
          }
        },
        "timeoutMs": 10000
      },
      {
        "name": "series",
        "title": "Metric series",
        "description": "Return one downsampled metric series for automation or external dashboards.",
        "inputSchema": {
          "type": "object",
          "properties": {
            "metric": {
              "type": "string",
              "title": "Metric",
              "default": "cpu"
            },
            "range": {
              "type": "string",
              "title": "Range",
              "default": "1h"
            },
            "maxPoints": {
              "type": "number",
              "title": "Max chart points",
              "default": 240
            }
          }
        },
        "timeoutMs": 5000
      },
      {
        "name": "summary",
        "title": "Metric summary",
        "description": "Return min, average, and max summaries for a time range.",
        "timeoutMs": 5000
      },
      {
        "name": "status",
        "title": "Collector status",
        "description": "Return local collector state.",
        "timeoutMs": 3000
      },
      {
        "name": "storage",
        "title": "Storage status",
        "description": "Return SQLite storage size, sample counts, and retention information.",
        "timeoutMs": 3000
      },
      {
        "name": "cleanup",
        "title": "Cleanup history",
        "description": "Remove samples older than the configured retention.",
        "timeoutMs": 5000
      },
      {
        "name": "vacuum",
        "title": "Vacuum storage",
        "description": "Run SQLite VACUUM after cleanup or large retention changes.",
        "timeoutMs": 10000
      }
    ],
    "webPanels": [
      {
        "id": "dashboard",
        "title": "System Monitor",
        "placement": "monitor",
        "aggregateCommand": "panel-data",
        "timeoutMs": 10000
      }
    ],
    "diagnostics": true
  },
  "settings": [
    {
      "key": "sampleIntervalMs",
      "label": "Sample interval",
      "type": "number",
      "description": "Preferred sample interval in milliseconds. The host scheduler may clamp very small values.",
      "default": 5000
    },
    {
      "key": "retentionDays",
      "label": "Retention days",
      "type": "number",
      "description": "How many days of SQLite metrics history are kept in the plugin data directory.",
      "default": 30
    },
    {
      "key": "maxChartPoints",
      "label": "Max chart points",
      "type": "number",
      "description": "Maximum downsampled points returned for each chart series.",
      "default": 240
    },
    {
      "key": "cleanupIntervalMinutes",
      "label": "Cleanup interval",
      "type": "number",
      "description": "Minimum minutes between automatic retention cleanup runs.",
      "default": 30
    },
    {
      "key": "autoRefreshMs",
      "label": "Panel auto refresh",
      "type": "number",
      "description": "Default auto-refresh interval used by the WebUI panel.",
      "default": 10000
    },
    {
      "key": "trackDisks",
      "label": "Track disks",
      "type": "boolean",
      "description": "Collect disk usage from mounted filesystems or drives.",
      "default": true
    },
    {
      "key": "trackNetworkInterfaces",
      "label": "Track network interfaces",
      "type": "boolean",
      "description": "Collect network byte counters and throughput.",
      "default": true
    }
  ]
}
